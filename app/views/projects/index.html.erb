<h2>My Projects</h2>

<% @projects.each do |project| %>
  <div>
    <%= link_to project.name, project_path(project) %>
    - Deadline: <%= project.deadline %>
  </div>
<% end %>

<%= link_to "New Project", new_project_path if current_user.admin? || current_user.manager? %>
